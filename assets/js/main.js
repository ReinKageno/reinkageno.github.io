const jsButton = document.getElementById('journalButton');

jsButton.addEventListener('touchstart', () => {
    jsButton.style.color = '#0d6efd';
});

jsButtom.addEventListener('touchend', () => {
    jsButton.style.color = '#ffffff';
});